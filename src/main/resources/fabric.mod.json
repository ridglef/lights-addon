{
  "schemaVersion": 1,
  "id": "lights-addon",
  "version": "${version}",

  "name": "Lights Addon",
  "description": "Homeassistant Light Integration",

  "license": "ARR",

  "environment": "client",
  "mixins": [
    "lights-addon.mixins.json"
  ],

  "depends": {
    "fabricloader": ">=${loader_version}",
    "minecraft": "${minecraft_version}"
  }
}
